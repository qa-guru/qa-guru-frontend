import { styled } from "@mui/system";
import { Paper } from "@mui/material";

export const StyledContentBox = styled(Paper)({
  height: "calc(100dvh - 190px)",
  margin: "20px 0 30px",
  padding: "15px 20px 20px",
});
