import { FC } from "react";

import ScrollPageSection from "features/scroll-page-section";

const ScrollPageSectionPage: FC = () => {
  return <ScrollPageSection />;
};

export default ScrollPageSectionPage;
