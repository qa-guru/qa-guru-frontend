import { FC } from "react";
import SetNewPassword from "features/authorization/containers/set-new-password";

const SetPasswordPage: FC = () => {
  return <SetNewPassword />;
};

export default SetPasswordPage;
