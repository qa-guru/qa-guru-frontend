mutation startTest($lectureId: ID!, $trainingId: ID!) {
  startTest(lectureId: $lectureId, trainingId: $trainingId) {
    id
    startTime
    endTime
    successfulCount
    errorsCount
    result
  }
}
