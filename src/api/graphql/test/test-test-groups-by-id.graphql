query testTestGroupsById($id: ID) {
  testTestGroupsById(id: $id) {
    id
    testName
    successThreshold
    testQuestions {
      id
      text
      testAnswers {
        id
        text
      }
    }
  }
}
