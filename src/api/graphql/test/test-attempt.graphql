query testAttempt($id: ID!) {
  testAttempt(id: $id) {
    id
    startTime
    endTime
    successfulCount
    errorsCount
    result
    testGroup {
      id
      testName
      successThreshold
      testQuestions {
        id
        text
        testAnswers {
          id
          text
        }
      }
    }
    testAttemptQuestionResults {
      testQuestion {
        id
        text
      }
      result
      testAnswerResults {
        testAnswer {
          id
          text
        }
        result
        answer
      }
    }
  }
}
