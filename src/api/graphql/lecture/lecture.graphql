query lecture($id: ID!) {
  lecture(id: $id) {
    id
    speakers {
      id
      firstName
      email
      lastName
      avatar
      rating {
        rating
      }
    }
    subject
    description
    content
  }
}
