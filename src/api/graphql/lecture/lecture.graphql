query lecture($id: ID!) {
  lecture(id: $id) {
    id
    speakers {
      id
      firstName
      email
      lastName
      middleName
      avatar
      rating {
        rating
      }
    }
    subject
    homeWorkLevel {
      id
      code
      description
      estimate
    }
    description
    content
  }
}
