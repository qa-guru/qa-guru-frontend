query userById($id: ID) {
  userById(id: $id) {
    id
    email
    firstName
    lastName
    middleName
    phoneNumber
    avatarLocation
    roles
    locked
    rating {
      rating
    }
  }
}
