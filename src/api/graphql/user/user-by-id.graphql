query userById($id: ID) {
  userById(id: $id) {
    id
    email
    firstName
    lastName
    roles
    git
    telegram
    stackOverflow
    linkedin
    avatar
    creationDate
    rating {
      rating
    }
  }
}
