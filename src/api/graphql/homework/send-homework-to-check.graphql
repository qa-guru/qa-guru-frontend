mutation sendHomeWorkToCheck(
  $lectureId: ID!
  $content: String!
  $trainingId: ID!
) {
  sendHomeWorkToCheck(
    lectureId: $lectureId
    content: $content
    trainingId: $trainingId
  ) {
    id
  }
}
