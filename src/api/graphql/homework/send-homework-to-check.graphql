mutation sendHomeWorkToCheck(
  $lectureId: ID!
  $content: String!
  $trainingId: ID!
) {
  sendHomeWorkToCheck(
    lectureId: $lectureId
    content: $content
    trainingId: $trainingId
  ) {
    id
    lecture {
      id
      subject
      description
    }
    answer
    status
    student {
      id
      firstName
      middleName
      avatar
      lastName
      rating {
        rating
      }
    }
    mentor {
      id
      firstName
      middleName
      avatar
      lastName
      rating {
        rating
      }
    }
    startCheckingDate
    endCheckingDate
  }
}
