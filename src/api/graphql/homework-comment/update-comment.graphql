mutation updateComment($id: ID!, $content: String!) {
  updateComment(id: $id, content: $content) {
    id
    creator {
      id
      email
      firstName
      middleName
      lastName
    }
    creationDate
    content
    homeWork {
      id
      answer
      status
    }
  }
}
