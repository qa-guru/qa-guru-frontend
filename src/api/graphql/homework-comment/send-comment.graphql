mutation sendComment($homeWorkId: ID!, $content: String!) {
  sendComment(homeWorkId: $homeWorkId, content: $content) {
    id
    creator {
      id
      email
      firstName
      middleName
      lastName
      rating {
        rating
      }
    }
    creationDate
    content
    homeWork {
      id
      answer
      status
    }
  }
}
