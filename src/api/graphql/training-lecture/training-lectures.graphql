query trainingLectures($id: ID!) {
  trainingLectures(id: $id) {
    id
    number
    locking
    availableFrom
    isAvailable
    lecture {
      id
      subject
      description
      creationDate
      speakers {
        id
        firstName
        lastName
      }
    }
  }
}
