mutation updateTrainingLecture($id: ID!, $lectureIds: [ID!]) {
  updateTrainingLecture(id: $id, lectureIds: $lectureIds) {
    id
    number
    lecture {
      id
      subject
      description
      creationDate
      speakers {
        id
        firstName
        lastName
        middleName
      }
    }
    lastLecture {
      id
      subject
      description
      creationDate
      speakers {
        id
        firstName
        lastName
        middleName
      }
    }
    locking
  }
}
