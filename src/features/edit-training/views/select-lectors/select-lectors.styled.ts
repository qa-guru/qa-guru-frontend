import { styled } from "@mui/system";
import { Chip } from "@mui/material";

export const StyledChip = styled(Chip)({
  padding: "10px",
});

export const StyledOptionChip = styled(Chip)({
  border: "none",
});
