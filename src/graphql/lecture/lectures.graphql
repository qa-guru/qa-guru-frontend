query lectures {
  lectures(page: 0, size: 10, sort: { field: SUBJECT }) {
    items {
      id
      speakers {
        id
        firstName
        middleName
        lastName
      }
      description
      homeWorkLevel {
        id
        code
        description
        estimate
      }
    }
    totalPages
    totalElements
  }
}
