query users {
  users(page: 0, size: 10, sort: { field: EMAIL, order: DESC }) {
    items {
      id
      email
      firstName
      middleName
      lastName
      phoneNumber
      locked
    }
    totalPages
    totalElements
  }
}
