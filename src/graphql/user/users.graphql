query Users {
  users(page: 0, size: 100, sort: { field: EMAIL, order: DESC }) {
    items {
      id
      email
      roles
      person {
        id
        firstName
        lastName
        middleName
        phoneNumber
        avatarLocation
      }
    }
    totalPages
    totalElements
  }
}
