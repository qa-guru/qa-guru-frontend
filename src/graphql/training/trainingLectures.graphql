query TrainingLectures($id: ID!) {
  trainingLectures(id: $id) {
    number
    lecture {
      id
      subject
      description
      speakers {
        person {
          firstName
          lastName
          middleName
          avatarLocation
        }
      }
    }
  }
}
