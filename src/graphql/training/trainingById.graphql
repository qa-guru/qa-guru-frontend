query TrainingById($id: ID!) {
  training(id: $id) {
    id
    name
    techStack
    lectures {
      id
      lecture {
        id
        subject
        description
      }
      locking
      lastLecture {
        id
        subject
        description
      }
    }
  }
}
